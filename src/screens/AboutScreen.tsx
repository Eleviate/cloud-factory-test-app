import React from "react"
import { SafeAreaView, Text, TouchableOpacity } from "react-native"

export default () => {
    return <SafeAreaView>
        <Text>О приложнии</Text>
        
    </SafeAreaView>
}